{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); echo \"$f\" | grep -qE '\\.cs$' && cd \"c:/Users/FURKAN/Desktop/JobAnalyzer\" && dotnet build --no-restore -v q 2>&1 | tail -8 || true",
            "timeout": 30,
            "statusMessage": "Derleme kontrol ediliyor..."
          }
        ]
      }
    ]
  }
}
